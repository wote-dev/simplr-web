# Cloudflare Pages SPA redirect rules
# This ensures that all routes are handled by React Router

# Serve static assets directly
/assets/*  /assets/:splat  200

# Redirect all other requests to index.html for client-side routing
# Exclude files with extensions to prevent infinite loops
/*    /index.html   200!